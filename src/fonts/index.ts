import { Inter, Lexend_Deca } from "next/font/google";

export const sans = Inter({
  variable: "--font-sans-default",
  subsets: ["latin"],
});

export const heading = Lexend_Deca({
  variable: "--font-heading-default",
  subsets: ["latin"],
});
